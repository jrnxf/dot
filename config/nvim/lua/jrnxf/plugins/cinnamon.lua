-- smooth scrolling plugin
require('cinnamon').setup({
  extra_keymaps = true,
  hide_cursor = true,
  centered = true, -- keep lines centered
  default_delay = 5, -- 5ms between each line (a bit faster than default of 7)
})
